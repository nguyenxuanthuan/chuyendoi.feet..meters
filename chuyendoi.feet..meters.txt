<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Bài t?p</title>
</head>
<body>
<h2>Chuy?n d?i feet and meter</h2>
<form>
    <p>Amount
        <input type="text" name="amount" id="amount"></p>
    From Unit
    <select name="from_unit" id="from_unit" value="feet">
        <option value="feet">FEET</option>
        <option value="meter">METER</option>
    </select>
    To Unit
    <select name="to_unit" id="ddlUnit">
        <option value="meter">METER</option>
        <option value="feet">FEET</option>
    </select>
    <button type="button" id ="btnConvert" onclick="computations()">Convert</button>
</form>
<script>
    function computations () {
        var amount = document.getElementById("amount").value;
        var from_unit = document.getElementById("from_unit").value;
        var to_unit = document.getElementById("ddlUnit").value;
        var result;
        if (from_unit === "feet" && to_unit === "meter") {
            result = amount * 3.279;
        } else if (from_unit === "meter" && to_unit === "feet")
        {
            result = amount * 0.305 ;
        }alert(result);
    }
</script>

</body>
</html>